<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" crossorigin="anonymous">
        <title>Create Issue | {{appName}}</title>
    </head>
    <body>
        <div class="container p-5">
            <div class="card card-body bg-light">
                <h1>New Issue for <i>{{repository}}</i></h1>
                {{#missingFields}}
                    <div class="alert alert-danger">
                        Not all fields were filled out!
                    </div>
                {{/missingFields}}
                {{#secretIncorrect}}
                    <div class="alert alert-danger">
                        The Anti-Spam-Secret is incorrect!
                    </div>
                {{/secretIncorrect}}
                {{#tooShort}}
                    <div class="alert alert-danger">
                        The title must be more than 15 chars and the description msut be more than 50 chars long! 
                    </div>
                {{/tooShort}}
                {{#unknownError}}
                    <div class="alert alert-warning">
                        An unknown error occured. Please contact the administrator.
                    </div>
                {{/unknownError}}
                {{#issueCreatedSuccessfully}}
                    <div class="alert alert-success">
                        The issue was created successfully! You can view it <a href="{{issueURL}}" target="_blank">here</a>.<br><br>
                        If you want, you can <a href="/{{urlKey}}">create another issue</a>.
                    </div>
                {{/issueCreatedSuccessfully}}
                {{^issueCreatedSuccessfully}}
                <form method="POST">
                    <div class="form-group row">
                        <label for="title" class="col-sm-2 col-form-label">Title</label>
                        <div class="col-sm-10">
                            <input type="text" name="title" class="form-control" id="title" value="{{title}}" placeholder="The title of the issue">
                        </div>
                    </div>
                    <div class="form-group row">
                        <label for="description" class="col-sm-2 col-form-label">Description</label>
                        <div class="col-sm-10">
                            <textarea name="description" class="form-control" id="description" placeholder="Description of the problem">{{description}}</textarea>
                        </div>
                    </div>
                    <div class="form-group row">
                        <label for="secret" class="col-sm-2 col-form-label">Secret</label>
                        <div class="col-sm-10">
                            <input type="password" name="secret" class="form-control" id="secret" value="" placeholder="Anti-Spam-Secret">
                        </div>
                    </div>
                    <div class="form-group row">
                        <label for="description" class="col-sm-2 col-form-label">Submit</label>
                        <div class="col-sm-10">
                            <input type="submit" class="btn btn-primary" name="submit" value="Submit">
                        </div>
                    </div>
                </form>
                {{/issueCreatedSuccessfully}}
                <small class="text-right">&copy; Hannes Rüger, 2020</small>
            </div>
        </div>
    </body>
</html>